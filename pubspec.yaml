name: usafe_front_end
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  # This matches your Dart 3.6.1 version
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # The contact package you added
  flutter_contacts: ^1.1.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Updated lints to match modern Flutter
  flutter_lints: ^5.0.0

flutter:
  # This line is REQUIRED for icons to work:
  uses-material-design: true

  assets:
    - assets/usafe_logo.png
    - assets/google.jpg
